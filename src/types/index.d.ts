export enum Gender {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
}

export enum SubscriptionType {
  PRIVATE = 'PRIVATE',
  NOT_PRIVATE = 'NOT_PRIVATE',
}

export enum SubscriptionStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  PENDING = 'PENDING',
}
